{
  "config": {
    "step": {
      "user": {
        "title": "MQTT Universal Media Player",
        "description": "Creates media players from MQTT discovery messages published under the discovery prefix (topic media_player/ID/config).",
        "data": {
          "discovery_prefix": "Discovery prefix"
        }
      }
    },
    "error": {
      "invalid_prefix": "The prefix must not be empty and must not contain + or #."
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only a single configuration is possible."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "discovery_prefix": "Discovery prefix"
        }
      }
    },
    "error": {
      "invalid_prefix": "The prefix must not be empty and must not contain + or #."
    }
  }
}
